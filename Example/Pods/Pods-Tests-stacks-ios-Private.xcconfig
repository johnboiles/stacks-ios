#include "Pods-Tests-stacks-ios.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/stacks-ios" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Expecta" "${PODS_ROOT}/Headers/Specta" "${PODS_ROOT}/Headers/stacks-ios"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}